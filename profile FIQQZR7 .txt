<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FIQQZR7 • Contact & Support</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #00ff88;
            --primary-glow: #00ff88;
            --secondary: #0088ff;
            --accent: #ff0088;
            --warning: #ffaa00;
            --dark: #0a0a0f;
            --darker: #050508;
            --light: #1a1a2e;
            --text: #ffffff;
            --text-light: #b0b0b0;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Courier New', monospace;
            background: var(--dark);
            color: var(--text);
            min-height: 100vh;
            overflow-x: hidden;
            position: relative;
        }

        /* Hacker Background */
        .matrix-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -3;
            opacity: 0.05;
        }

        /* Scanning Effects */
        .scan-line {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 1px;
            background: linear-gradient(90deg, transparent, var(--primary), transparent);
            z-index: -2;
            animation: scan 4s linear infinite;
            box-shadow: 0 0 10px var(--primary);
        }

        @keyframes scan {
            0% { top: 0; opacity: 0.8; }
            50% { opacity: 1; }
            100% { top: 100%; opacity: 0.8; }
        }

        /* Grid Pattern */
        .grid-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: 
                linear-gradient(rgba(0, 255, 136, 0.02) 1px, transparent 1px),
                linear-gradient(90deg, rgba(0, 255, 136, 0.02) 1px, transparent 1px);
            background-size: 40px 40px;
            z-index: -2;
            animation: gridMove 30s linear infinite;
        }

        @keyframes gridMove {
            0% { transform: translate(0, 0); }
            100% { transform: translate(40px, 40px); }
        }

        /* Loading Screen */
        .loading-screen {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--darker);
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 9999;
            flex-direction: column;
            transition: opacity 0.8s ease-out;
        }

        .loading-screen.fade-out {
            opacity: 0;
            pointer-events: none;
        }

        .loading-text {
            font-size: 2.5rem;
            font-weight: bold;
            margin-bottom: 40px;
            position: relative;
            text-align: center;
        }

        .loading-text .line {
            display: block;
            overflow: hidden;
            height: 40px;
        }

        .loading-text span {
            display: inline-block;
            transform: translateY(100%);
            animation: slideUp 0.5s ease-out forwards;
        }

        .loading-text .line:nth-child(1) span { animation-delay: 0.2s; }
        .loading-text .line:nth-child(2) span { animation-delay: 0.6s; }

        @keyframes slideUp {
            to { transform: translateY(0); }
        }

        .loading-progress {
            width: 400px;
            height: 3px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 3px;
            overflow: hidden;
            position: relative;
        }

        .loading-progress::after {
            content: '';
            position: absolute;
            height: 100%;
            width: 0%;
            background: linear-gradient(90deg, var(--primary), var(--secondary));
            animation: loading 2.5s ease-in-out forwards;
            box-shadow: 0 0 10px var(--primary);
        }

        @keyframes loading {
            0% { width: 0%; }
            100% { width: 100%; }
        }

        /* Audio Controls */
        .audio-controls {
            position: fixed;
            bottom: 20px;
            right: 20px;
            z-index: 1000;
            background: rgba(26, 26, 46, 0.9);
            border-radius: 50px;
            padding: 10px;
            border: 1px solid var(--primary);
            backdrop-filter: blur(10px);
            display: flex;
            align-items: center;
            gap: 10px;
            animation: slideInRight 0.5s ease-out 3s forwards;
            opacity: 0;
            transform: translateX(100px);
        }

        @keyframes slideInRight {
            to { opacity: 1; transform: translateX(0); }
        }

        .music-info {
            font-size: 0.8rem;
            color: var(--primary);
            margin-right: 10px;
            white-space: nowrap;
        }

        .music-control {
            background: transparent;
            border: none;
            color: var(--primary);
            font-size: 1.2rem;
            cursor: pointer;
            padding: 5px;
            border-radius: 50%;
            transition: all 0.3s;
        }

        .music-control:hover {
            background: rgba(0, 255, 136, 0.2);
            transform: scale(1.1);
        }

        /* Main Container */
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
            opacity: 0;
            animation: fadeIn 0.8s ease-out 2.8s forwards;
        }

        @keyframes fadeIn {
            to { opacity: 1; }
        }

        /* Header */
        .header {
            text-align: center;
            padding: 60px 20px 40px;
            position: relative;
            margin-bottom: 40px;
        }

        .header::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 300px;
            height: 300px;
            background: radial-gradient(circle, var(--primary) 0%, transparent 70%);
            opacity: 0.1;
            filter: blur(60px);
            animation: pulseGlow 6s ease-in-out infinite;
        }

        @keyframes pulseGlow {
            0%, 100% { opacity: 0.1; transform: translate(-50%, -50%) scale(1); }
            50% { opacity: 0.2; transform: translate(-50%, -50%) scale(1.1); }
        }

        .logo {
            font-size: 4.5rem;
            font-weight: 900;
            text-transform: uppercase;
            letter-spacing: 6px;
            position: relative;
            margin-bottom: 20px;
            animation: glitch 5s infinite;
        }

        @keyframes glitch {
            0%, 95%, 100% { 
                text-shadow: 
                    0 0 10px var(--primary),
                    0 0 20px var(--primary),
                    0 0 30px var(--primary);
                transform: translate(0);
                clip-path: inset(0 0 0 0);
            }
            1% { 
                transform: translate(-3px, 3px);
                clip-path: inset(20% 0 30% 0);
            }
            2% { 
                transform: translate(3px, -3px);
                clip-path: inset(10% 0 40% 0);
            }
            96% { 
                opacity: 0.9;
            }
        }

        .logo span {
            background: linear-gradient(45deg, var(--primary), var(--secondary), var(--accent));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            position: relative;
        }

        .tagline {
            font-size: 1.4rem;
            color: var(--text-light);
            margin-bottom: 30px;
            letter-spacing: 4px;
            position: relative;
            display: inline-block;
            padding: 10px 30px;
            border: 1px solid rgba(0, 255, 136, 0.3);
            border-radius: 50px;
            background: rgba(0, 255, 136, 0.05);
            animation: taglinePulse 3s infinite;
        }

        @keyframes taglinePulse {
            0%, 100% { border-color: rgba(0, 255, 136, 0.3); }
            50% { border-color: rgba(0, 255, 136, 0.6); }
        }

        /* Main Content Grid */
        .content-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 40px;
            margin-bottom: 60px;
        }

        @media (max-width: 992px) {
            .content-grid {
                grid-template-columns: 1fr;
            }
        }

        /* Profile Card */
        .profile-card {
            background: rgba(26, 26, 46, 0.7);
            backdrop-filter: blur(15px);
            border-radius: 25px;
            padding: 40px;
            border: 1px solid rgba(0, 255, 136, 0.2);
            position: relative;
            overflow: hidden;
            transition: all 0.4s ease;
        }

        .profile-card:hover {
            border-color: var(--primary);
            box-shadow: 
                0 0 30px rgba(0, 255, 136, 0.1),
                inset 0 0 30px rgba(0, 255, 136, 0.05);
            transform: translateY(-5px);
        }

        .profile-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, var(--primary), var(--secondary), var(--accent));
            animation: borderFlow 3s linear infinite;
        }

        @keyframes borderFlow {
            0% { background-position: 0% 50%; }
            100% { background-position: 100% 50%; }
        }

        /* Avatar Section */
        .avatar-section {
            text-align: center;
            margin-bottom: 40px;
        }

        .avatar-container {
            position: relative;
            display: inline-block;
            margin-bottom: 25px;
        }

        .avatar {
            width: 180px;
            height: 180px;
            border-radius: 50%;
            border: 4px solid transparent;
            background: linear-gradient(45deg, var(--primary), var(--secondary), var(--accent)) border-box;
            -webkit-mask: 
                linear-gradient(#fff 0 0) padding-box, 
                linear-gradient(#fff 0 0);
            mask: 
                linear-gradient(#fff 0 0) padding-box, 
                linear-gradient(#fff 0 0);
            -webkit-mask-composite: xor;
            mask-composite: exclude;
            padding: 4px;
            animation: rotate 20s linear infinite;
            box-shadow: 
                0 0 30px rgba(0, 255, 136, 0.3),
                inset 0 0 30px rgba(0, 255, 136, 0.1);
        }

        .avatar img {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            object-fit: cover;
        }

        .avatar-ring {
            position: absolute;
            top: -10px;
            left: -10px;
            right: -10px;
            bottom: -10px;
            border: 2px solid rgba(0, 255, 136, 0.3);
            border-radius: 50%;
            animation: pulseRing 3s ease-in-out infinite;
        }

        @keyframes pulseRing {
            0%, 100% { transform: scale(1); opacity: 0.5; }
            50% { transform: scale(1.05); opacity: 0.8; }
        }

        .status {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
            margin-top: 15px;
        }

        .status-dot {
            width: 14px;
            height: 14px;
            border-radius: 50%;
            background: var(--primary);
            position: relative;
            animation: statusPulse 2s infinite;
        }

        @keyframes statusPulse {
            0%, 100% { 
                box-shadow: 
                    0 0 5px var(--primary),
                    0 0 10px var(--primary),
                    0 0 15px var(--primary);
            }
            50% { 
                box-shadow: 
                    0 0 10px var(--primary),
                    0 0 20px var(--primary),
                    0 0 30px var(--primary);
            }
        }

        /* Social Links */
        .social-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 15px;
            margin-top: 30px;
        }

        @media (max-width: 576px) {
            .social-grid {
                grid-template-columns: 1fr;
            }
        }

        .social-card {
            display: flex;
            align-items: center;
            gap: 15px;
            padding: 18px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 15px;
            text-decoration: none;
            color: var(--text);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .social-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
            transition: left 0.6s;
        }

        .social-card:hover::before {
            left: 100%;
        }

        .social-card:hover {
            transform: translateY(-3px);
            border-color: var(--primary);
            background: rgba(0, 255, 136, 0.05);
        }

        .social-icon {
            width: 45px;
            height: 45px;
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            transition: all 0.3s;
        }

        .social-card:hover .social-icon {
            transform: scale(1.1) rotate(5deg);
        }

        .social-card.roblox .social-icon { 
            background: linear-gradient(45deg, #ff4655, #ff6b6b); 
            color: white;
        }
        .social-card.tiktok .social-icon { 
            background: linear-gradient(45deg, #000000, #00f2ea); 
            color: white;
        }
        .social-card.discord .social-icon { 
            background: linear-gradient(45deg, #5865f2, #4752c4); 
            color: white;
        }

        .social-info {
            flex: 1;
        }

        .social-name {
            font-weight: bold;
            font-size: 1.1rem;
            margin-bottom: 5px;
        }

        .social-handle {
            font-size: 0.9rem;
            color: var(--text-light);
        }

        /* Payment Methods Section */
        .payment-section {
            background: rgba(26, 26, 46, 0.7);
            backdrop-filter: blur(15px);
            border-radius: 25px;
            padding: 40px;
            border: 1px solid rgba(0, 255, 136, 0.2);
            position: relative;
            overflow: hidden;
        }

        .payment-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, var(--accent), var(--warning), var(--primary));
            animation: borderFlow 4s linear infinite;
        }

        .section-title {
            font-size: 2rem;
            margin-bottom: 40px;
            color: var(--primary);
            position: relative;
            padding-bottom: 15px;
            text-align: center;
        }

        .section-title::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 100px;
            height: 3px;
            background: linear-gradient(90deg, transparent, var(--primary), transparent);
        }

        /* Payment Methods */
        .payment-methods {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
        }

        .payment-card {
            background: rgba(0, 0, 0, 0.3);
            border-radius: 20px;
            padding: 30px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            transition: all 0.3s ease;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .payment-card:hover {
            border-color: var(--primary);
            transform: translateY(-5px);
            box-shadow: 0 15px 35px rgba(0, 255, 136, 0.1);
        }

        .payment-icon {
            font-size: 3rem;
            margin-bottom: 25px;
            color: var(--primary);
            position: relative;
            display: inline-block;
        }

        .payment-icon::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
            width: 50px;
            height: 3px;
            background: linear-gradient(90deg, transparent, var(--primary), transparent);
        }

        .payment-title {
            font-size: 1.5rem;
            margin-bottom: 20px;
            color: var(--text);
        }

        /* QR Code */
        .qr-container {
            background: white;
            padding: 15px;
            border-radius: 15px;
            display: inline-block;
            margin-bottom: 25px;
            border: 3px solid var(--primary);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
            animation: qrFloat 3s ease-in-out infinite;
        }

        @keyframes qrFloat {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }

        .qr-code {
            width: 200px;
            height: 200px;
            display: block;
            border-radius: 10px;
        }

        /* Phone Number */
        .phone-number {
            background: rgba(0, 255, 136, 0.1);
            border: 2px solid var(--primary);
            border-radius: 50px;
            padding: 15px 30px;
            font-size: 1.3rem;
            font-weight: bold;
            color: var(--primary);
            display: inline-flex;
            align-items: center;
            gap: 15px;
            margin: 25px 0;
            cursor: pointer;
            transition: all 0.3s;
            position: relative;
            overflow: hidden;
        }

        .phone-number::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(0, 255, 136, 0.2), transparent);
            transition: left 0.5s;
        }

        .phone-number:hover::before {
            left: 100%;
        }

        .phone-number:hover {
            background: rgba(0, 255, 136, 0.2);
            transform: scale(1.05);
            box-shadow: 0 10px 20px rgba(0, 255, 136, 0.2);
        }

        /* Copy Button */
        .copy-btn {
            background: linear-gradient(45deg, var(--primary), var(--secondary));
            border: none;
            padding: 12px 25px;
            border-radius: 10px;
            color: white;
            font-family: 'Courier New', monospace;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            gap: 10px;
            margin: 0 auto;
        }

        .copy-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(0, 255, 136, 0.3);
        }

        .copy-btn:active {
            transform: translateY(0);
        }

        /* Instructions */
        .instructions {
            margin-top: 30px;
            padding: 20px;
            background: rgba(0, 0, 0, 0.3);
            border-radius: 15px;
            border-left: 4px solid var(--warning);
        }

        .instructions-title {
            color: var(--warning);
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .instructions-list {
            list-style: none;
            padding-left: 0;
        }

        .instructions-list li {
            margin-bottom: 10px;
            padding-left: 25px;
            position: relative;
            color: var(--text-light);
        }

        .instructions-list li::before {
            content: '▶';
            position: absolute;
            left: 0;
            color: var(--primary);
        }

        /* Terminal */
        .terminal {
            background: rgba(0, 0, 0, 0.8);
            border-radius: 15px;
            padding: 25px;
            margin-top: 40px;
            border: 1px solid var(--primary);
            font-family: 'Courier New', monospace;
            position: relative;
            overflow: hidden;
        }

        .terminal::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 3px;
            background: linear-gradient(90deg, var(--primary), var(--secondary), var(--accent));
            animation: terminalScan 4s linear infinite;
        }

        @keyframes terminalScan {
            0% { transform: translateX(-100%); }
            100% { transform: translateX(100%); }
        }

        .terminal-output {
            min-height: 150px;
            overflow-y: auto;
        }

        .terminal-line {
            margin-bottom: 8px;
            opacity: 0;
            animation: typeLine 0.1s forwards;
            white-space: pre-wrap;
            word-break: break-all;
        }

        @keyframes typeLine {
            to { opacity: 1; }
        }

        .prompt {
            color: var(--primary);
        }

        .command {
            color: var(--text);
        }

        .response {
            color: var(--text-light);
        }

        .cursor {
            display: inline-block;
            width: 8px;
            height: 20px;
            background: var(--primary);
            margin-left: 5px;
            animation: blink 1s infinite;
            vertical-align: middle;
        }

        @keyframes blink {
            0%, 100% { opacity: 1; }
            50% { opacity: 0; }
        }

        /* Footer */
        .footer {
            text-align: center;
            padding: 40px 20px;
            margin-top: 60px;
            border-top: 1px solid rgba(0, 255, 136, 0.2);
            color: var(--text-light);
            position: relative;
        }

        .footer::before {
            content: '';
            position: absolute;
            top: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 150px;
            height: 3px;
            background: linear-gradient(90deg, transparent, var(--primary), transparent);
        }

        /* Notification */
        .notification {
            position: fixed;
            bottom: 30px;
            left: 50%;
            transform: translateX(-50%) translateY(100px);
            background: linear-gradient(45deg, var(--primary), var(--secondary));
            color: white;
            padding: 15px 30px;
            border-radius: 50px;
            font-weight: bold;
            z-index: 10000;
            opacity: 0;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            gap: 10px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        }

        .notification.show {
            opacity: 1;
            transform: translateX(-50%) translateY(0);
        }

        /* Responsive */
        @media (max-width: 768px) {
            .logo { font-size: 2.8rem; letter-spacing: 3px; }
            .tagline { font-size: 1.1rem; }
            .profile-card, .payment-section { padding: 25px; }
            .qr-code { width: 180px; height: 180px; }
            .phone-number { font-size: 1.1rem; padding: 12px 25px; }
            .loading-text { font-size: 1.8rem; }
            .loading-progress { width: 300px; }
        }

        @media (max-width: 480px) {
            .container { padding: 15px; }
            .content-grid { gap: 25px; }
            .logo { font-size: 2.2rem; }
            .tagline { font-size: 1rem; padding: 8px 20px; }
            .qr-code { width: 160px; height: 160px; }
        }

        /* Custom Scrollbar */
        ::-webkit-scrollbar {
            width: 10px;
        }

        ::-webkit-scrollbar-track {
            background: var(--darker);
        }

        ::-webkit-scrollbar-thumb {
            background: linear-gradient(var(--primary), var(--secondary));
            border-radius: 5px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: linear-gradient(var(--secondary), var(--primary));
        }
    </style>
</head>
<body>
    <!-- Hacker Background -->
    <canvas class="matrix-bg" id="matrixCanvas"></canvas>
    <div class="scan-line"></div>
    <div class="grid-overlay"></div>

    <!-- Loading Screen -->
    <div class="loading-screen" id="loadingScreen">
        <div class="loading-text">
            <div class="line"><span>SYSTEM BOOT</span></div>
            <div class="line"><span>FIQQZR7 PROFILE v4.0</span></div>
        </div>
        <div class="loading-progress"></div>
    </div>

    <!-- Audio Controls -->
    <div class="audio-controls">
        <div class="music-info" id="musicInfo">MEMORIZING - FIQQZR7</div>
        <button class="music-control" onclick="toggleMusic()" id="playBtn">
            <i class="fas fa-play"></i>
        </button>
        <button class="music-control" onclick="stopMusic()">
            <i class="fas fa-stop"></i>
        </button>
        <input type="range" min="0" max="100" value="50" class="volume-slider" id="volumeSlider" 
               oninput="changeVolume(this.value)" style="width: 80px;">
    </div>

    <!-- Main Container -->
    <div class="container">
        <!-- Header -->
        <div class="header">
            <h1 class="logo"><span>FIQQZR7</span></h1>
            <p class="tagline">CONTACT & SUPPORT</p>
        </div>

        <!-- Main Content -->
        <div class="content-grid">
            <!-- Left Column: Profile -->
            <div class="profile-card">
                <div class="avatar-section">
                    <div class="avatar-container">
                        <div class="avatar">
                            <img src="https://tr.rbxcdn.com/bc2f562e4c0e86bb9c7691ab13f1e171/420/420/Avatar/Png" 
                                 alt="FIQQZR7"
                                 onerror="this.src='https://api.dicebear.com/7.x/adventurer/svg?seed=FIQQZR7&backgroundColor=00ff88'">
                        </div>
                        <div class="avatar-ring"></div>
                    </div>
                    <h2 style="font-size: 2rem; margin-bottom: 10px;">FIQQZR7</h2>
                    <div class="status">
                        <div class="status-dot"></div>
                        <span>Premium Script Developer • Online</span>
                    </div>
                </div>

                <div class="section-title" style="text-align: left; margin-bottom: 30px;">CONNECT</div>

                <div class="social-grid">
                    <a href="https://www.roblox.com/users/324325914/profile" target="_blank" class="social-card roblox">
                        <div class="social-icon">
                            <i class="fab fa-roblox"></i>
                        </div>
                        <div class="social-info">
                            <div class="social-name">Roblox</div>
                            <div class="social-handle">@FIQQZR7</div>
                        </div>
                        <i class="fas fa-external-link-alt" style="color: var(--text-light);"></i>
                    </a>

                    <a href="https://www.tiktok.com/@fiqqzr7exploits" target="_blank" class="social-card tiktok">
                        <div class="social-icon">
                            <i class="fab fa-tiktok"></i>
                        </div>
                        <div class="social-info">
                            <div class="social-name">TikTok</div>
                            <div class="social-handle">@fiqqzr7exploits</div>
                        </div>
                        <i class="fas fa-external-link-alt" style="color: var(--text-light);"></i>
                    </a>

                    <a href="https://discord.gg/s5cpqbNnc" target="_blank" class="social-card discord">
                        <div class="social-icon">
                            <i class="fab fa-discord"></i>
                        </div>
                        <div class="social-info">
                            <div class="social-name">Discord</div>
                            <div class="social-handle">Join Server</div>
                        </div>
                        <i class="fas fa-external-link-alt" style="color: var(--text-light);"></i>
                    </a>

                    <a href="https://github.com/fiqqzr7-cloud" target="_blank" class="social-card">
                        <div class="social-icon" style="background: linear-gradient(45deg, #333, #666);">
                            <i class="fab fa-github"></i>
                        </div>
                        <div class="social-info">
                            <div class="social-name">GitHub</div>
                            <div class="social-handle">Script Repository</div>
                        </div>
                        <i class="fas fa-external-link-alt" style="color: var(--text-light);"></i>
                    </a>
                </div>

                <div class="terminal">
                    <div class="terminal-output" id="terminalOutput">
                        <div class="terminal-line"><span class="prompt">root@fiqqzr7:~$</span> <span class="command">system_status</span></div>
                        <div class="terminal-line"><span class="response">System: ONLINE</span></div>
                        <div class="terminal-line"><span class="response">Services: ACTIVE</span></div>
                        <div class="terminal-line"><span class="response">Support: 24/7</span></div>
                        <div class="terminal-line"><span class="response">Last Login: Today 14:30</span></div>
                        <div class="terminal-line"><span class="prompt">root@fiqqzr7:~$</span> <span class="cursor"></span></div>
                    </div>
                </div>
            </div>

            <!-- Right Column: Payment Methods -->
            <div class="payment-section">
                <h2 class="section-title">PAYMENT METHODS</h2>

                <div class="payment-methods">
                    <!-- TNG Wallet -->
                    <div class="payment-card">
                        <div class="payment-icon">
                            <i class="fas fa-wallet"></i>
                        </div>
                        <h3 class="payment-title">Touch 'n Go eWallet</h3>
                        
                        <div class="qr-container">
                            <img src="https://api.qrserver.com/v1/create-qr-code/?size=200x200&data=https%3A%2F%2Ftngdigital.com.my&color=00ff88&bgcolor=0a0a0f&margin=10" 
                                 alt="TNG QR Code" 
                                 class="qr-code">
                        </div>
                        
                        <div class="phone-number" onclick="copyToClipboard('+60123456789')">
                            <i class="fas fa-phone-alt"></i>
                            <span id="phoneText">+60 12-345 6789</span>
                        </div>
                        
                        <button class="copy-btn" onclick="copyToClipboard('+60123456789')">
                            <i class="fas fa-copy"></i> Copy Number
                        </button>
                    </div>

                    <!-- WhatsApp -->
                    <div class="payment-card">
                        <div class="payment-icon">
                            <i class="fab fa-whatsapp"></i>
                        </div>
                        <h3 class="payment-title">WhatsApp Business</h3>
                        
                        <div class="qr-container">
                            <img src="https://api.qrserver.com/v1/create-qr-code/?size=200x200&data=https%3A%2F%2Fwa.me%2F60123456789&color=25D366&bgcolor=0a0a0f&margin=10" 
                                 alt="WhatsApp QR Code" 
                                 class="qr-code">
                        </div>
                        
                        <div class="phone-number" onclick="copyToClipboard('+60123456789')">
                            <i class="fab fa-whatsapp"></i>
                            <span id="whatsappText">+60 12-345 6789</span>
                        </div>
                        
                        <button class="copy-btn" onclick="openWhatsApp()">
                            <i class="fab fa-whatsapp"></i> Open WhatsApp
                        </button>
                    </div>
                </div>

                <div class="instructions">
                    <h3 class="instructions-title">
                        <i class="fas fa-info-circle"></i> Payment Instructions
                    </h3>
                    <ul class="instructions-list">
                        <li>Scan QR code or use phone number above</li>
                        <li>Send payment with reference "FIQQZR7"</li>
                        <li>Screenshot payment confirmation</li>
                        <li>Send screenshot to @FIQQZR7 on Discord</li>
                        <li>Receive your premium script within 24 hours</li>
                    </ul>
                </div>
            </div>
        </div>

        <!-- Footer -->
        <div class="footer">
            <p>© 2024 FIQQZR7 • Premium Script Services</p>
            <p style="margin-top: 15px; font-size: 0.9rem; color: rgba(255,255,255,0.5);">
                <i class="fas fa-shield-alt"></i> Secure Transactions • 24/7 Support
            </p>
        </div>
    </div>

    <!-- Notification -->
    <div class="notification" id="notification">
        <i class="fas fa-check-circle"></i>
        <span id="notificationText">Copied to clipboard!</span>
    </div>

    <!-- Audio -->
    <audio id="backgroundMusic" loop>
        <source src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3" type="audio/mpeg">
        Your browser does not support the audio element.
    </audio>

    <script>
        // Matrix Background
        const canvas = document.getElementById('matrixCanvas');
        const ctx = canvas.getContext('2d');
        
        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;
        
        const chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789$+-*/=%"\'#&_(),.;:?!\\|{}<>[]^~';
        const fontSize = 16;
        const columns = canvas.width / fontSize;
        const drops = [];
        const speeds = [];
        
        for(let i = 0; i < columns; i++) {
            drops[i] = Math.floor(Math.random() * canvas.height / fontSize) * fontSize;
            speeds[i] = Math.random() * 2 + 0.5;
        }
        
        function drawMatrix() {
            ctx.fillStyle = 'rgba(10, 10, 20, 0.05)';
            ctx.fillRect(0, 0, canvas.width, canvas.height);
            
            ctx.fillStyle = '#00ff88';
            ctx.font = `bold ${fontSize}px 'Courier New', monospace`;
            
            for(let i = 0; i < drops.length; i++) {
                const text = chars[Math.floor(Math.random() * chars.length)];
                const alpha = Math.random() * 0.7 + 0.3;
                ctx.globalAlpha = alpha;
                ctx.fillText(text, i * fontSize, drops[i] * fontSize);
                
                if(drops[i] * fontSize > canvas.height && Math.random() > 0.97) {
                    drops[i] = 0;
                }
                drops[i] += speeds[i];
            }
        }

        // Music Controls
        const music = document.getElementById('backgroundMusic');
        const playBtn = document.getElementById('playBtn');
        const volumeSlider = document.getElementById('volumeSlider');
        const musicInfo = document.getElementById('musicInfo');

        function toggleMusic() {
            if (music.paused) {
                music.play();
                playBtn.innerHTML = '<i class="fas fa-pause"></i>';
                musicInfo.innerHTML = 'MEMORIZING - FIQQZR7 🔊';
                showNotification('Music playing: MEMORIZING');
            } else {
                music.pause();
                playBtn.innerHTML = '<i class="fas fa-play"></i>';
                musicInfo.innerHTML = 'MEMORIZING - FIQQZR7';
                showNotification('Music paused');
            }
        }

        function stopMusic() {
            music.pause();
            music.currentTime = 0;
            playBtn.innerHTML = '<i class="fas fa-play"></i>';
            musicInfo.innerHTML = 'MEMORIZING - FIQQZR7';
            showNotification('Music stopped');
        }

        function changeVolume(value) {
            music.volume = value / 100;
            if (value > 0 && music.paused) {
                music.play();
                playBtn.innerHTML = '<i class="fas fa-pause"></i>';
            }
        }

        // Auto-play music after loading
        setTimeout(() => {
            music.volume = 0.5;
            music.play().then(() => {
                playBtn.innerHTML = '<i class="fas fa-pause"></i>';
                musicInfo.innerHTML = 'MEMORIZING - FIQQZR7 🔊';
            }).catch(e => {
                console.log("Autoplay prevented:", e);
            });
        }, 2800);

        // Loading Screen
        setTimeout(() => {
            const loadingScreen = document.getElementById('loadingScreen');
            loadingScreen.classList.add('fade-out');
            setTimeout(() => {
                loadingScreen.style.display = 'none';
            }, 800);
        }, 2500);

        // Terminal Animation
        function typeTerminal() {
            const terminal = document.getElementById('terminalOutput');
            const lines = [
                {prompt: "root@fiqqzr7:~$", command: "whoami", response: "User: FIQQZR7"},
                {prompt: "root@fiqqzr7:~$", command: "uptime", response: "System uptime: 24 days"},
                {prompt: "root@fiqqzr7:~$", command: "check_services", response: "All services: ACTIVE"},
                {prompt: "root@fiqqzr7:~$", command: "status --payments", response: "Payment systems: ONLINE"},
                {prompt: "root@fiqqzr7:~$", command: "", response: ""}
            ];

            let lineIndex = 5; // Start after initial lines

            function addLine() {
                if (lineIndex < lines.length) {
                    const line = document.createElement('div');
                    line.className = 'terminal-line';
                    line.style.animationDelay = '0s';
                    
                    const lineData = lines[lineIndex];
                    line.innerHTML = `
                        ${lineData.prompt ? `<span class="prompt">${lineData.prompt}</span> ` : ''}
                        ${lineData.command ? `<span class="command">${lineData.command}</span>` : ''}
                        ${lineData.response ? `<div class="response">${lineData.response}</div>` : ''}
                    `;
                    
                    terminal.appendChild(line);
                    terminal.scrollTop = terminal.scrollHeight;
                    lineIndex++;
                    
                    setTimeout(addLine, 1500);
                }
            }

            setTimeout(addLine, 3000);
        }

        // Copy to Clipboard
        function copyToClipboard(text) {
            navigator.clipboard.writeText(text).then(() => {
                showNotification(`Copied: ${text}`);
            }).catch(err => {
                showNotification('Failed to copy. Please copy manually.');
            });
        }

        // Open WhatsApp
        function openWhatsApp() {
            window.open('https://wa.me/60123456789?text=Hello%20FIQQZR7%2C%20I%20want%20to%20purchase%20premium%20scripts', '_blank');
        }

        // Show Notification
        function showNotification(message) {
            const notification = document.getElementById('notification');
            const notificationText = document.getElementById('notificationText');
            
            notificationText.textContent = message;
            notification.classList.add('show');
            
            setTimeout(() => {
                notification.classList.remove('show');
            }, 3000);
        }

        // Animate elements on scroll
        function animateOnScroll() {
            const cards = document.querySelectorAll('.profile-card, .payment-card, .social-card');
            
            cards.forEach((card, index) => {
                const cardTop = card.getBoundingClientRect().top;
                const windowHeight = window.innerHeight;
                
                if (cardTop < windowHeight - 100) {
                    card.style.opacity = '1';
                    card.style.transform = 'translateY(0)';
                }
            });
        }

        // Initialize
        document.addEventListener('DOMContentLoaded', () => {
            // Start matrix animation
            setInterval(drawMatrix, 50);
            
            // Start terminal typing
            setTimeout(typeTerminal, 3500);
            
            // Initial scroll animation
            setTimeout(animateOnScroll, 3200);
            
            // Window resize handler
            window.addEventListener('resize', () => {
                canvas.width = window.innerWidth;
                canvas.height = window.innerHeight;
            });
            
            // Scroll event listener
            window.addEventListener('scroll', animateOnScroll);
            
            // Add hover effects to payment cards
            const paymentCards = document.querySelectorAll('.payment-card');
            paymentCards.forEach(card => {
                card.addEventListener('mouseenter', () => {
                    const icon = card.querySelector('.payment-icon i');
                    if (icon) {
                        icon.style.transform = 'scale(1.2) rotate(10deg)';
                        icon.style.transition = 'all 0.3s ease';
                    }
                });
                
                card.addEventListener('mouseleave', () => {
                    const icon = card.querySelector('.payment-icon i');
                    if (icon) {
                        icon.style.transform = 'scale(1) rotate(0deg)';
                    }
                });
            });
            
            // Add click effect to copy buttons
            const copyButtons = document.querySelectorAll('.copy-btn, .phone-number');
            copyButtons.forEach(btn => {
                btn.addEventListener('click', function(e) {
                    e.stopPropagation();
                    this.style.transform = 'scale(0.95)';
                    setTimeout(() => {
                        this.style.transform = '';
                    }, 200);
                });
            });
            
            // Add keyboard shortcuts
            document.addEventListener('keydown', (e) => {
                // Space to toggle music
                if (e.code === 'Space' && !e.target.matches('input, textarea')) {
                    e.preventDefault();
                    toggleMusic();
                }
                
                // Escape to stop music
                if (e.code === 'Escape') {
                    stopMusic();
                }
                
                // M to mute/unmute
                if (e.code === 'KeyM' && e.ctrlKey) {
                    e.preventDefault();
                    if (music.volume > 0) {
                        music.volume = 0;
                        showNotification('Music muted');
                    } else {
                        music.volume = volumeSlider.value / 100;
                        showNotification('Music unmuted');
                    }
                }
            });
            
            // Add glitch effect to logo randomly
            setInterval(() => {
                if (Math.random() < 0.3) {
                    const logo = document.querySelector('.logo');
                    logo.style.animation = 'none';
                    setTimeout(() => {
                        logo.style.animation = 'glitch 5s infinite';
                    }, 10);
                }
            }, 10000);
        });

        // Add particle effect on click
        document.addEventListener('click', (e) => {
            if (e.target.matches('.copy-btn, .phone-number, .music-control')) return;
            
            for(let i = 0; i < 5; i++) {
                const particle = document.createElement('div');
                particle.style.cssText = `
                    position: fixed;
                    width: 4px;
                    height: 4px;
                    background: var(--primary);
                    border-radius: 50%;
                    pointer-events: none;
                    z-index: 9999;
                    left: ${e.clientX}px;
                    top: ${e.clientY}px;
                    animation: particleExplode 0.6s ease-out forwards;
                `;
                
                document.body.appendChild(particle);
                
                setTimeout(() => {
                    particle.remove();
                }, 600);
            }
            
            // Add particle animation to style
            if (!document.querySelector('#particle-animation')) {
                const style = document.createElement('style');
                style.id = 'particle-animation';
                style.textContent = `
                    @keyframes particleExplode {
                        0% {
                            transform: translate(0, 0) scale(1);
                            opacity: 1;
                        }
                        100% {
                            transform: translate(${Math.random() * 100 - 50}px, ${Math.random() * 100 - 50}px) scale(0);
                            opacity: 0;
                        }
                    }
                `;
                document.head.appendChild(style);
            }
        });
    </script>
</body>
</html>